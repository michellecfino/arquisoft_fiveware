_format_version: "3.0"
_transform: true

services:
  - name: manejador-service
    url: http://manejador_reportes:5000
    routes:
      - name: reportes-route
        paths:
          - /reportes

  - name: rabbitmq-mgmt-service
    url: http://rabbitmq:15672
    routes:
      - name: rabbitmq-mgmt-route
        paths:
          - /rabbitmq-admin
EOF